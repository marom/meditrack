# Autowire Spring test constructors so test classes use constructor injection
# (matching the project's constructor-injection-everywhere rule) instead of
# field injection.
spring.test.constructor.autowire.mode = all
